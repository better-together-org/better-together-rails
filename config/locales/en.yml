---
en:
  helpers:
    select:
      prompt: Please select
    submit:
      create: Create %{model}
      submit: Save %{model}
      update: Update %{model}
  time:
    am: am
    formats:
      default: "%Y-%m-%d %H:%M:%S %z"
      long: "%B %d, %Y %H:%M"
      short: "%b %d %H:%M"
    pm: pm
