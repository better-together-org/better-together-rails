
<%# locals: (form:) %>
<div class="tour-stops-fields nested-fields border-top py-3 my-3 row align-items-end" data-new-record="<%= form.object.new_record? %>">
  <h4><%= TourStop.name.humanize %></h4>
  <%= form.hidden_field :_destroy %>
  <%= form.hidden_field :tour_id %>

  <div class="col-md-12 mb-3">
    <%= form.label :venue_id, Venue.name.humanize %>
    <%= form.collection_select :venue_id, bookable_venues, :id, :name, { }, { class: 'form-select', required: true, data: { controller: "better_together--slim-select" } } %>
  </div>

  <div class="col-8 col-md-10 mt-3 mt-md-0">
    <%= link_to '#remove-tour-artist', class: 'btn btn-outline-danger btn-sm', data: { action: 'click->better_together--dynamic-fields#remove' } do %>
      <i class="fas fa-minus"></i> <%= t('better_together.remove') %>
    <% end %>
  </div>
</div>
