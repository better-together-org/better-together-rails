<div class="col mb-4">
  <div id="<%= dom_id deal_type %>" class="card shadow-sm">
    <div class="card-body">
      <h5 class="card-title"><%= deal_type.name %></h5>
      <p class="card-text"><%= deal_type.description %></p>
      <a href="<%= deal_type_path(deal_type) %>" class="btn btn-primary">View Details</a>
    </div>
  </div>
</div>
