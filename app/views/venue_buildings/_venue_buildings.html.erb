
<%# locals: (form: nil) %>

<div class="venue-buildings mb-4" data-controller="better_together--dynamic-fields">
  <h3><%= VenueBuilding.name.humanize.pluralize %></h3>

  <div id="<%= dom_id(form.object, :venue_buildings) %>" class="mb-3" data-better_together--dynamic-fields-target="container">
    <%= form.fields_for :venue_buildings do |venue_building_form| %>
      <%= render 'venue_buildings/fields', form: venue_building_form %>
    <% end %>
  </div>

  <!-- Hidden template -->
  <template data-better_together--dynamic-fields-target="template">
    <%# Build a new PhoneNumber instance %>
    <%= form.fields_for :venue_buildings, form.object.venue_buildings.build, child_index: 'NEW_RECORD' do |venue_building_form| %>
      <%= render 'venue_buildings/fields', form: venue_building_form %>
    <% end %>
  </template>

  <%= link_to '#', class: 'btn btn-outline-primary btn-sm', data: { action: 'click->better_together--dynamic-fields#add' } do %>
    <i class="fas fa-plus"></i> <%= t('better_together.global.add') %>
  <% end %>
</div>
