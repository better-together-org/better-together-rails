
<%# locals: (form: nil) %>

<div class="venue-offer-deal-types mb-4" data-controller="better_together--dynamic-fields">
  <h5><%= VenueOfferDealType.name.humanize.pluralize %></h5>

  <div id="<%= dom_id(form.object, :venue_offer_deal_types) %>" class="mb-3 row" data-better_together--dynamic-fields-target="container">
    <%= form.fields_for :venue_offer_deal_types, VenueOfferDealType.new(venue_offer_id: form.object.id) do |venue_offer_deal_type_form| %>
      <%= render 'venue_offer_deal_types/fields', form: venue_offer_deal_type_form %>
    <% end %>
  </div>

  <!-- Hidden template -->
  <template data-better_together--dynamic-fields-target="template">
    <%# Build a new PhoneNumber instance %>
    <%= form.fields_for :venue_offer_deal_types, form.object.venue_offer_deal_types.build, child_index: 'NEW_RECORD' do |venue_offer_deal_type_form| %>
      <%= render 'venue_offer_deal_types/fields', form: venue_offer_deal_type_form %>
    <% end %>
  </template>

  <%= link_to '#', class: 'btn btn-outline-primary btn-sm', data: { action: 'click->better_together--dynamic-fields#add' } do %>
    <i class="fas fa-plus"></i> <%= t('better_together.global.add') %>
  <% end %>
</div>
